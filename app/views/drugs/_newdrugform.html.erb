<%= simple_form_for Drug.new  do |f| %>
  <div class="form-inputs p-4">
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <div class="create p-4">
      <%= f.button :submit, "Create Drug", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
