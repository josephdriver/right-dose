<div class="wrapper d-flex">
  <%= render 'sidebar' %>
  <div class="container-fluid px-0">
    <div class="main-content">
      <%= render 'header' %>
      <div class="d-flex justify-content-around md-margin-btm">
        <%= render 'organizationAgeandCalcs' %>
      </div>
      <div class="d-flex justify-content-around md-margin-btm">
        <%= render 'organizationOverview' %>
      </div>
    </div>
  </div>
</div>
